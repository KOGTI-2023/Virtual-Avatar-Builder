# Required configuration
# (No required credentials at this time; keep for future secrets.)

# Optional: Override the directory for the lowdb JSON database
DATABASE_DIR=./data

# Optional: External API proxy targets used by Next.js rewrites
EXTERNAL_API_URL_SERVICE1=https://service1.example.com
EXTERNAL_API_URL_SERVICE2=https://service2.example.com
WEATHER_API_URL=https://weather.example.com
EXTERNAL_STT_API_URL=https://stt.example.com
